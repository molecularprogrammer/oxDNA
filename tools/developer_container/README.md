# developer_container

This is a toolchain for Ubuntu Linux.

## Local Docker Build

This can be used to test changes before publishing them, if needed.

Run `./build.sh`.

## Publish Docker Image

Merge a branch to `main` to build and publish the image to the container registry.
